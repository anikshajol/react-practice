// import React from "react";

const add = (first, second) => {
  return first + second;
};

// export default Calculator;

const multiply = (first, second) => {
  return first * second;
};

export { add, multiply };
